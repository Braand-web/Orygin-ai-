# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@orygin-ai/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@orygin-ai/dsh-tool-session-query'
          - id: timeout-policy
            name: '@orygin-ai/dsh-tool-call-timeout-policy'
