# Exercise the model-facing LSP tool through the shipped ACP app and Loader entry path.
# The scenario workspace supplies the deterministic stdio server used by this test composition.
- id: base
  name: '@orygin-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - insert:
          - id: lsp
            name: '@orygin-ai/dsh-lsp'
          - id: lsp-stdio
            name: '@orygin-ai/dsh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@orygin-ai/dsh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@orygin-ai/dsh-tool-lsp'
            config:
              maxLocations: 1
